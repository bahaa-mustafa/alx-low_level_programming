#include "main.h"
#include<string.h>

/**
 * _strlen - return the length of a string
 * @s: the string
 *
 * Return: number of string length
 */

int _strlen(char *s)
{
	int length = strlen(*s);

	return (length);
}
